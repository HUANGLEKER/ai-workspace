# Pinned to versions verified working on Python 3.14 (major upgrade from the
# original 0.3.x langchain / 0.6.x chromadb baseline). See README for notes.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# LangChain (1.x)
langchain==1.3.4
langchain-community==0.4.2
langchain-openai==1.2.2
langchain-core==1.4.0
langchain-text-splitters==1.1.2
langgraph==1.2.4

# MCP (Model Context Protocol) — used by the agent to load tools from SSE MCP servers.
# Requires the AI service runtime (WSL/Linux/Docker), not exercised on native Windows.
langchain-mcp-adapters==0.1.9
mcp==1.16.0

# Vector DB
chromadb==1.5.9

# Redis
redis==8.0.0

# MinIO
minio==7.2.20

# Document parsing
pypdf==6.12.2
python-docx==1.2.0
unstructured==0.18.32

# HTTP client
httpx==0.28.1

# Utilities
tiktoken==0.13.0
