# 开发环境变量（仅在 npm run dev 时生效）
# Vite 会将以 VITE_ 前缀的变量暴露给浏览器端代码（import.meta.env.VITE_*）

# 应用标题，显示在浏览器标签页
VITE_APP_TITLE=AI Workspace

# Spring Boot 后端地址；开发时 Vite DevServer 通过 proxy 将 /api 转发到此地址，
# 生产构建中此变量不生效（由 Nginx 反向代理处理跨域）
VITE_API_BASE_URL=http://localhost:8080
